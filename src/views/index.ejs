<!DOCTYPE html>
<html>
  <head>
    <title>Colores</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Mesaje</h1>
    <!-- aca poner el mensaje que dice el entregable -->
    <% if (typeof data != "undefined") { %>
      <p>Hola <%=data.name %>, elegiste el color: <%=data.color %>, tu email es: <%= data.email %>
              <%= data.edad !='' ? 'y tu edad es:' + data.edad: null %>
      </p>
      <% } %>
    <div id="errores">
      <!-- <h2>Errores</h2>    ASÍ LO HIZO PRIMERO PERO LUEGO LO DEJÓ COMO FIGURA DEBAJO:
    <!-- aca ponemos los errores del validator -->
      <!-- <% if (typeof errors != undefined) { %>
        <% errors.forEach(error => { %>
          <span style="color: red"><li><%= error.msg %></li></span>
          <% }) %>
      <% } %> -->
 -->
    <h2>ERRORES</h2>
    <% if(typeof errors!='undefined' ){ %>
      <ul>
        <% for (let i=0; i < errors.length; i++) { %>
          <li style="color: red;">
            <%= errors[i].msg %>
          </li>
          <% } %>
      </ul>
      <% } %>  

    </div>
    
    <br>
    <br>

    <form action="/" method="POST">
      <div>
        <label for="">Ingrese su nombre</label>
        <input type="text" name="name" >
      </div>
      
      <div>
        <label for="">Seleccione un color</label>
        <select name="color">
          <option value="">Elegí un color</option>
          <option value="yellow">Amarillo</option>
          <option value="blue">Azul</option>
          <option value="red">Rojo</option>
          <option value="green">Verde</option>
        </select>
      </div>
      
      <div>
        <label for="">Ingrese su mail</label>
        <input type="email" name="email" id="" >
      </div>

      <div>
        <label for="">Ingrese su edad</label>
        <input type="number" name="edad" id="">
      </div>

      <div>
        <label for="">Recordar color</label>
        <input type="checkbox" name="recordar_color" id="">
      </div>

      <button type="submit">Enviar</button>
    </form>

    <button><a href="/borrar">Olvidar Color</a></button>

  </body>
</html>
